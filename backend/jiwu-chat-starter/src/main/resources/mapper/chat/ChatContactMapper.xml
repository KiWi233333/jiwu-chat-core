<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiwu.api.common.main.mapper.chat.ChatContactMapper">

    <insert id="refreshOrCreateActiveTime">
        insert into chat_contact(`room_id`,`user_id`,`last_msg_id`,`active_time`)
        values
        <foreach collection="memberUidList" item="uid" separator=",">
            (#{roomId},#{uid},#{msgId},#{activeTime})
        </foreach>
        on DUPLICATE KEY UPDATE
        `last_msg_id` = VALUES(`last_msg_id`),
        `active_time` = VALUES(`active_time`)
    </insert>

    <insert id="refreshOrCreateReadTime">
        insert into chat_contact(`room_id`,`user_id`, `read_time`)
        values
        <foreach collection="memberUidList" item="uid" separator=",">
            (#{roomId},#{uid},#{readTime})
        </foreach>
        on DUPLICATE KEY UPDATE
        `read_time` = VALUES(`read_time`)
    </insert>
</mapper>
